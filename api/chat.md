## Chat api

#### one-to-one chat(must be friends)

```json
{
    "cmd": "single_chat",
    "token": "yang",
    "to": "to user name",
    "msg": "msg body"
}
```

#### Group chat

```json
{
    "cmd": "group_chat",
    "token": "yang",
    "to": "to room name",
    "msg": "msg body"
}
```
