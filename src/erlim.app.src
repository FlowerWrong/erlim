{application, erlim,
    [
        {description, "An im server written in erlang."},
        {vsn, "1"},
        {registered, [erlim_sup]},
        {applications, [kernel, stdlib, sasl]},
        {mod, {erlim_app, []}},
        {env, []}
    ]
}.
